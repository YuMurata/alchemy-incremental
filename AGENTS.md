# AGENTS.md

このプロジェクトは、AIエージェントと人間が共同でインクリメンタルゲームを開発するためのプロジェクトです。

## 基本方針
- **Docker環境開発**: 全てのビルド・実行は Docker コンテナ内 (`docker compose`) で行う。
- **コードベース開発**: GUIエディタではなく、コードの修正によってロジックを構築する。
- **軽量化**: 数値の増大によるラグを防ぐため、ループ処理ではなく数学的なレートベースの計算を行う。
- **レシピ主導開発**: 300個のレシピを核とし、コンテンツの充実を最優先する。

## エージェントへの指示 (Hermes Agent)
- **分析不要**: 実行結果の分析は行わず、コード修正のみに専念する。
- **Docker徹底**: ターミナル操作は必ず `docker compose exec app <command>` を経由する。
- **ビルドの徹底**: 修正後は必ずビルドが通るコードを提供し、検証は人間側で行う。
- **ロードマップ遵守**: 第1マイルストーン（最小クリアループ）から第2マイルストーン（300個のレシピ）へ順次移行する。
