# Alchemy Incremental 開発ルール

## 開発方針
- **設計書は正**: 実装はすべて設計書（`docs/`配下）の定義に従う。
- **指示の優先順位**:
  1. ぷりんさんからの直接指示
  2. 設計書のルール
  3. 開発ロードマップ

## UI/UX 設計ルール
- **操作導線**: 素材と釜の物理距離を最小化し、マウス移動量を最適化する。
- **演出**: UIは静的な数字パネルではなく、相棒が動くライブ演出を重視する。
- **レビューの記録**: UI改善や辛口レビューの指摘事項は、必ず `docs/design/frontend/UI_review_summary.md` に追記し、設計にフィードバックを反映させる。
- **拡張性**: レシピが増えても破綻しないタブ検索やクイックアクセスを構築する。

## 実装・Git運用ルール
- **Git Auto-Push**: プッシュ時はスキル `git-auto-push` を使用する。正常に動作することが確認できた修正結果は、忘れずにプッシュする。
- **検証**: ビルドコマンドは実行せず、コードと設計による論理検証を優先する。
- **コミット**: 節目の作業ごとにこまめにコミット・プッシュする。

## 品質管理
- ユーザーレビュー（専門家視点）の結果は無視せず、即座にドキュメントに反映し、改善案を設計に組み込む。
- **修正報告フォーマット**: 指摘対応時は以下のテーブルで報告を行い、作業内容を `obsidian` の開発ログへバックアップする。

| 指摘箇所 | 対応状況 | 備考 |
| :--- | :--- | :--- |
| 指摘内容 | 完了/未対応 | 実装の詳細や、あえて対応しなかった理由など |
