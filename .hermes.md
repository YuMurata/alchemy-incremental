# alchemy-incremental 開発統制事項

## 開発方針
- **設計書は正**: 実装はすべて設計書（`references/`配下）の定義に従う。
- **指示の優先順位**:\n  1. ぷりんさんからの直接指示\n  2. 設計書のルール\n  3. 開発ロードマップ

## 開発・検証環境の構築・運用

本プロジェクトでは以下のコンテナ構成を用いて開発・検証を行う。

- `engine`: バックエンドコア（合成エンジン）。
- `cli`: デバッグ用インターフェース。
- `webui`: フロントエンド。

### テストプレイ・検証手順
1. **コンテナ起動**: `docker compose up -d`
2. **検証実行**: `docker compose exec -T cli npx tsc && node src/index.js`

### トラブルシューティング
- **Node.jsモジュールエラー**: `import` 文の拡張子確認や `tsconfig.json` の設定を確認すること。
- **ビルド時のレート制限**: ローカルでのビルドコマンドは実行禁止。検証コマンドのみ実行すること。

- **テスト方針**:
  - るるが実装ロジックの単体テストをコード化する。
  - 大規模な結合テストやプレイ感の確認は、るるが作成したCLI/スクリプトを用いてぷりんさんが実行・評価する。
  - コンテキスト効率を優先し、小さな単位で検証を進める。
  - **レートリミット対策**: API制限(RPM/TPM/RPD)を考慮し、大量のコンテキスト消費やループ処理を避ける。テスト実行は最小限かつ軽量なロジックのみに留める。

## 設計フォルダ構成
- `docs/`: 設計書一式。
  - `00_概念設計書.md` (最上流)
  - `01_基本設計書.md`
  - `02_システム設計書.md`
  - `03_詳細設計書.md`
  - `design-doc.md` (集約・方針)
- `data/`: ゲームデータ (`recipe-db.json`)
- `src/`: ソースコード

