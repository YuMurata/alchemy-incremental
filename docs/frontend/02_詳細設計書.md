# 02_詳細設計書

## 1. API型定義 (TypeScript)
### `/api/mix` (POST)
```typescript
interface MixRequest {
  items: string[];
  spiritId: string;
}

interface MixResponse {
  success: boolean;
  resultItemId?: string;
  spiritExpGain: number;
  updateStrategy: 'replace' | 'patch'; // 更新戦略: replace=全体上書き, patch=累積加算
}
```

## 4. データ更新戦略
- **Strategy: replace**: インベントリ等、サーバーの状態が常に正しいリスト。
- **Strategy: patch**: 精霊EXP等、累積的な加算値。フロント側で現在の値に加算してストアを更新する。

### `/api/status` (GET)
```typescript
interface GameStatusResponse {
  inventory: { itemId: string; count: number }[];
  spirit: { id: string; level: number; exp: number };
  unlockedRecipes: string[];
}
```

## 2. 状態管理ストア (Context)
```typescript
interface GameStore {
  items: Record<string, number>;
  spirit: { level: number; exp: number };
  uiState: 'Idle' | 'Playing' | 'Success' | 'Failure' | 'Result' | 'Menu';
  // 更新アクション
  mix: (items: string[]) => Promise<void>;
}
```

## 3. コンポーネントProps定義
- **Overlay**: `state: GameStore['uiState']`
- **Mixer**: `onMix: (items: string[]) => void`
- **Inventory**: `items: Record<string, number>`, `onSelect: (id: string) => void`
